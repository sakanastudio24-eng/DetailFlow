from __future__ import annotations


def is_booking_rate_limited(client_ip: str | None) -> bool:
    """Placeholder for per-IP booking rate limiting (future phase)."""
    _ = client_ip
    return False
